name: Actions availability check

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Actions is working
        run: echo "GitHub Actions is enabled for this repository"
